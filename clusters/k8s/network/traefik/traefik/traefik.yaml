apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: traefik
  namespace: traefik
spec:
  interval: 5m
  chart:
    spec:
      # renovate: registryUrl=https://helm.traefik.io/traefik
      chart: traefik
      version: 9.12.3
      sourceRef:
        kind: HelmRepository
        name: traefik-charts
        namespace: flux-system
      interval: 5m
  values:
    deployment:
      kind: DaemonSet
    logs:
      general:
        level: DEBUG
    ingressClass:
      enabled: true
      isDefaultClass: true
    ingressRoute:
      dashboard:
        enabled: true
    additionalArguments:
      - "--entrypoints.websecure.http.tls"
      - "--ping"
      - "--api.dashboard=true"
      - "--metrics.prometheus=false"
      - "--providers.kubernetesingress.ingressendpoint.hostname=wintergrasp.icecrown.online"
    ports:
      web:
        redirectTo: websecure
    service:
      spec:
        externalTrafficPolicy: Local